local widgets = {}

widgets.cpu_widget = require("widgets.cpu_widget")
widgets.ram_widget = require("widgets.ram_widget")
widgets.volume_widget = require("widgets.volume_widget")
widgets.mic_widget = require("widgets.mic_widget")
widgets.clock_widget = require("widgets.clock_widget")

return widgets
